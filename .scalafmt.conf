version = 3.11.5
runner.dialect = scala3
maxColumn = 120

# Formatting is intentionally mechanical. Semantic rewrites belong in a named
# Scalafix rule only when the compiler and domain types cannot express them.
rewrite.rules = []

# This source is reproduced byte-for-byte from the pinned external receipt.
project.excludeFilters = [
  "modules/mvpa/shared/src/test/scala/scalafim/fmri/mvpa/scenarios/MvpaExternalReferenceFixture\\.scala"
]
