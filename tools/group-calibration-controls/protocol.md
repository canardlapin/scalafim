# Controlled nuisance calibration protocol, v1

Owner ScalaFIM group calibration; independent-subject named scalar group coefficient. E[Y|X]=1.7 + beta_group*g + .8*z where present. Null beta_group=0; alternative .7; confidence-set inclusion at true nonzero .7 by inversion of exactly the same test. No joint F/spatial/adaptive-model admission. No new production default from this exploratory slice.

Compare OLS t; CR2 identity/Satterthwaite (existing failure); HC3 identity/Satterthwaite; CR2 using true marginal diagonal covariance target (diagnostic only); oracle WLS z and WLS t using true variance or true variance shape; null-restricted HC2 and HC3 bootstrap each with Rademacher and Mammen multipliers. Bootstrap adjustment divides restricted residual by (1-h_restricted)^(k/2), k=1 HC2 / k=2 HC3; full-model HCk studentization both observed and bootstrap. Fixed 1e-12 absolute tie tolerance; (1+exceedances)/(B+1). Working-target and oracle methods use simulation truth and are not deployable estimators. Do not treat normal-only oracle guarantees as valid for skew/t3.

Pilot grid fixed in protocol.json before calculations: six design settings (n20 quarter smooth; n20 balanced smooth; n20 quarter no covariate; n20 quarter high leverage z[0]+=5; n8 balanced smooth; n80 quarter smooth) crossed with flat/reverse variances and normal/skew errors =24 cells. Add spread and dominant precision-imbalance cases for n20/n80 quarter smooth, normal/skew =8; reverse t3 for n8/n20/n80 =3; reverse additive variance .2 for n8/n20/n80 normal/skew =6. Total41. Flat variance .52, reverse/spread .04..1, dominant variance 10 for subject0 and .04 others. Smooth z=cos((i+.5)*2pi/n), group first n/2 or max(2,n/4). Marginal errors standardized normal, t3 or lognormal; additive variance is a marginal scale component, not a nonnormal multilevel decomposition.

Pilot 2000 independent studies/cell,499 draws, roots outcomes2026091601/actions2026091602. Fresh confirmation 10000 studies/cell,999 draws, roots2026091701/02. Confirm each candidate's largest pilot null excess per normal/skew family (deduplicated), each candidate's lowest power at nominal alpha among normal cells, and mandatory original n20 quarter reverse normal, n8 balanced reverse normal, n80 quarter reverse skew, n20 balanced flat normal. Limit cost by selecting the largest-excess cells across four feasible corrected candidates only (HC3/Satt, three new bootstrap variants); retain all baseline methods and oracle methods on those same confirmations. Thus at most16 confirmation cells before deduplication. No selection based on favorable p values. Report every cell and no universal admission from absence of detected excess. Family-adjusted one-sided99.9% Clopper-Pearson lower bounds across feasible methods*cells identify definite inflation; pointwise95% intervals for plots. Coverage/power/finite failures retained. Pilot fixed before results; confirmation selection deterministic from pilot.

Estimated-variance experiment: actual Gaussian first-level error e~N(0,v_i) and independent vhat=v_i*chi2_df/df, plus independent Gaussian subject heterogeneity u~N(0,tau2). n20/n80 quarter smooth, variance spread/reverse, tau2=0/.2, df known/8/40 =24 cells x10000 studies. Outcomes/variance roots2026091801/02. Compare existing native PM/mKH using vhat, oracle WLS using v+tau2, plug-in GLS t using vhat+TRUE tau2 (diagnostic isolates variance estimation, not a proposed estimator), and corresponding HC3/Satterthwaite after whitening with plug-in variance. Report actual total uncertainty, not first-level df as group df. Preserve paired study identities. Retain explicit-input parity with R lm/clubSandwich/metafor and compile native probe against exact source snapshot. Native methods unchanged unless a genuine reproducible implementation defect appears.

Evidence: analytic controls and independent R fixture parity before interpreting simulations; explicit bootstrap multipliers for parity, no shared-seed claims; conditional moment diagnostics explain working-model covariance mismatch. Record fresh seeds, runtime/peak scratch sizes, scientific failure states, sources, plots and inspected/refined visual QA. Prior receipts preserved. Generic solvers remain in their providers. No app-local fitter. Final product/UI admission remains a separate ticket.
