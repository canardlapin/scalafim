# Predeclared group symmetry qualification — 2026-09-08

Frozen before new inference/calibration results. Target: a common subject-effect center mu at each spatial sample; with finite means this is the common mean. Subjects are independent, errors jointly invariant to independent subject sign changes conditional on fixed selection and, when used, precisions. Within-person differences must already be valid first-level contrasts, one per subject. No nuisance, repeated-row, arbitrary contrast/F-test or spatial multiplicity admission.

Statistic: absolute weighted residual sum / root sum of squared weighted residuals, using equal weights or fixed inverse first-level variance. Denominator is invariant on each orbit. Null center is explicit. Estimation is separately labeled weighted mean; no synthetic t df or SE. Exact complete sign group (n<=16); Monte Carlo independent uniform signs with replacement, identity added in p=(1+b)/(1+B). Conservative numerical tie tolerance. Reuse subject-bound actions across spatial blocks, refuse unsupported designs and changed subject sets, no samplewise dropping.

Independent reference: Python exhaustive enumeration and high precision tie cases. Fast JVM/JS regressions must include identity correction, exact granularity, symmetry/units/translation/row/block invariance, missing variance and unsupported design refusal, boundary/nonfinite/resource failures, and independent sign-pattern agreement.

Held-out generator seeds use 20260909 and 20260910 (previous grid used 20260908). Equal and unequal precision (.04..1, plus dominant precision ratio 100), known variance and independent chi-square df8/40 estimates; tau2=0,.2,1; n=8,20,80. Gaussian and symmetric t3 variance-one errors. Null mu=0, shifted-null mu=.7 (confidence-set inversion coverage), alternative mu=.35. Exact n8; MC >=999 draws for larger n. Report failures, binomial Monte Carlo intervals for independent studies, or cluster-by-plan intervals when actions shared across batches. Do not call shared-plan datasets independent MC experiments. Primary gate: no scenario has a simultaneous one-sided 99.9% lower confidence bound above .05; mathematical invariance and exact-orbit tests are primary, simulation is corroboration, not threshold tuning. Power is descriptive and compared on matched data to PM/mKH; no blanket dominance claim. No automatic consumer default before provenance/UI admission.

Resource evidence: separate plan-generation cost from execution; completed n20/80 spatial maps with fixed B, time/CPU/allocation and finite results. Cache only B*n signs, O(n) sample scratch, O(m) result maps, no B*m resampling distribution. Exact/MC product-path calibration and baseline PM/mKH recorded reproducibly. Confidence-set coverage means acceptance of true mu when tests are inverted; endpoint/connectedness algorithms are outside this slice.

Implementation refinement before calibration: reuse the already-pinned resample4s 6bc4172 public Rand/Seed streams directly (domain 1004 per replicate). Current ScalaFIM pins a pre-sign-action Multivar; upgrading its entire multivariate dependency would widen admission unnecessarily. Generic random generation stays in its existing resample4s owner; ScalaFIM maps unbiased binary draws to subject multipliers. Statistical sampling law is unchanged.
